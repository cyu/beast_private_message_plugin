A plugin that adds private messaging support for Beast.

INSTALL INSTRUCTIONS
==============================

1) Install this plugin.
2) Run the following command

      script/runner 'Beast::Plugins::PrivateMessage.install'
